{% extends 'base.html.twig' %}

{% block title %}Pokemons{% endblock %}

{% block body %}
    <h1>Pokemons</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokemon.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ pokemon.numero }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pokemon.nombre }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ pokemon.tipo }}</td>
            </tr>
            <tr>
                <th>Imagen</th>
                <td>{{ pokemon.imagen }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pokemons_index') }}">back to list</a>

    <a href="{{ path('app_pokemons_edit', {'id': pokemon.id}) }}">edit</a>

    {{ include('pokemons/_delete_form.html.twig') }}
{% endblock %}
